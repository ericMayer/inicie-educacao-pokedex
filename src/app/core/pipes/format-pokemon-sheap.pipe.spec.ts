import { FormatPokemonSheapPipe } from './format-pokemon-sheap.pipe';

describe('FormatPokemonSheapPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatPokemonSheapPipe();
    expect(pipe).toBeTruthy();
  });
});
